import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import ExifReaderComparison from './ExifReaderTest';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ExifReaderComparison />
    </React.StrictMode>
);
